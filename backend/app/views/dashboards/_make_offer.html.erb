<!-- Form for offering a listing to a hunter -->
<%= form_with controller: HunterProfile.offer do |form| %>
  <%= hidden_field(:listing, @Listing) %>
  <%= hidden_field(:hunter_profile, @HunterProfile) %>
  <%= form.submit %>
<% end %>
